# Frontend environment variables
# Rename this file to .env.local for local development

# Backend API base URL (without /api suffix)
# Leave empty in dev — Vite proxy handles /api → http://localhost:4000
# Set this to your deployed backend URL in production
VITE_API_URL=
